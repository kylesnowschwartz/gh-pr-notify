<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  launchd does NOT expand $HOME or ${HOME} in plist values.
  Replace YOUR_HOME below with your actual home directory path, e.g. /Users/kyle

  Install:
    sed "s|YOUR_HOME|$HOME|g" com.gh-pr-notify.plist > ~/Library/LaunchAgents/com.gh-pr-notify.plist
    launchctl load ~/Library/LaunchAgents/com.gh-pr-notify.plist
-->
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.gh-pr-notify</string>
	<key>ProgramArguments</key>
	<array>
		<string>YOUR_HOME/.local/bin/gh-pr-notify</string>
		<string>--log-file</string>
		<string>YOUR_HOME/.local/state/gh-pr-notify/gh-pr-notify.log</string>
	</array>
	<key>EnvironmentVariables</key>
	<dict>
		<key>PATH</key>
		<string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
	</dict>
	<key>RunAtLoad</key>
	<true/>
	<key>KeepAlive</key>
	<true/>
	<!--
	  gh-pr-notify writes its own rotated log via --log-file above. These paths
	  catch only what launchd itself reports, such as a binary that fails to
	  start, so they stay small.
	-->
	<key>StandardOutPath</key>
	<string>YOUR_HOME/.local/state/gh-pr-notify/launchd.log</string>
	<key>StandardErrorPath</key>
	<string>YOUR_HOME/.local/state/gh-pr-notify/launchd.log</string>
</dict>
</plist>
